# ----------------------------------------
# 📌 공통 안내
# 이 파일은 Git에 올라가지 않아야 하는 파일/폴더를 설정한 것입니다.
# 예: 자바 실행 결과, 개인 설정 파일, 임시파일 등은 Git에 올리지 않도록 했습니다.
# ----------------------------------------

# ✅ 자바 컴파일 결과물이 저장되는 폴더 (실행하면 자동 생성됨)
/out/

# ✅ IntelliJ에서 생성하는 프로젝트 설정 폴더 (개인별 설정이 달라서 무시)
/.idea/

# ✅ IntelliJ 모듈 설정 파일 (자동 생성되므로 Git에 올릴 필요 없음)
*.iml

# ✅ macOS에서 자동 생성되는 임시 파일
.DS_Store

# ✅ Windows에서 자동 생성되는 임시 파일
Thumbs.db
